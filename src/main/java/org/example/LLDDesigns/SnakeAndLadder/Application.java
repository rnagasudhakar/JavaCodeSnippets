package org.example.LLDDesigns.SnakeAndLadder;

public class Application {

    public static void main(String[] args) {

        // Improvements

        // updatedposition value can be fetched from Board class only

        // we can always convert hashmap to array too.

        Game game = new Game();
        game.startGame();
    }
}
